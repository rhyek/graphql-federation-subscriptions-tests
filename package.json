{
	"scripts": {
		"dev": "concurrently -n frontend,gateway,messages,users,subscriptions --kill-others \"(cd apps/frontend; npm run start)\" \"(cd apps/nest-apps; npm run start:dev gql-gateway)\" \"(cd apps/nest-apps; npm run start:dev gql-messages)\" \"(cd apps/nest-apps; npm run start:dev gql-users)\" \"(cd apps/nest-apps; npm run start:dev gql-subscriptions)\""
	},
	"devDependencies": {
		"concurrently": "^6.3.0"
	}
}
