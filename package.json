{
	"scripts": {
		"dev": "concurrently --kill-others \"pnpm --filter={apps/frontend} start\" \"pnpm --filter={apps/nest-apps} start:dev gql-main\""
	},
	"devDependencies": {
		"concurrently": "^6.3.0"
	}
}
